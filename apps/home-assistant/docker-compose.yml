services:
  home-assistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: home-assistant
    restart: unless-stopped
    privileged: false
    env_file:
      - ${CALEOPE_APP_CONFIG}/home-assistant/secrets.env
    environment:
      - TZ=Europe/Paris
    ports:
      - "${HA_PORT_WEB:-8123}:8123"
    volumes:
      - ${CALEOPE_APP_DATA}/home-assistant/config:/config
    networks:
      - caleope-public
      - caleope-internal

networks:
  caleope-public:
    external: true
  caleope-internal:
    external: true
