services:
  immich-server:
    image: ghcr.io/immich-app/immich-server:release
    container_name: immich-server
    restart: unless-stopped
    depends_on:
      - immich-db
      - immich-redis
    env_file:
      - app.env
      - {{.BaseDir}}/app-config/{{.AppID}}/secrets.env
    volumes:
      - {{.BaseDir}}/app-data/immich/library:/usr/src/app/upload
      # Miniatures sur le DISQUE LOCAL, même quand la bibliothèque est sur un
      # stockage réseau. Une galerie affiche des dizaines de vignettes par
      # écran : à 72 ms la lecture sur NFS (mesuré sur une vraie bibliothèque,
      # contre 14 ms en local), un écran de 50 coûte 3,6 s de lecture pure et la
      # navigation devient poussive. Les originaux, eux, restent sur le grand
      # volume : on ne les ouvre qu'un par un.
      # ⚠️ Sur une bibliothèque existante, RECOPIER les miniatures dans
      # app-data/immich/thumbs-local AVANT d'appliquer, sinon Immich les
      # considère absentes et les regénère toutes.
      - {{.BaseDir}}/app-data/immich/thumbs-local:/usr/src/app/upload/thumbs
      - /etc/localtime:/etc/localtime:ro
    {{- range .Ports}}
    ports:
      - "{{.Host}}:{{.Container}}"
    {{- end}}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.immich.rule=Host(`{{.Domain}}`)"
      - "traefik.http.routers.immich.entrypoints=websecure"
      - "traefik.http.routers.immich.tls=true"
      - "traefik.http.services.immich.loadbalancer.server.port=2283"
      - "traefik.docker.network=caleope-public"
    networks:
      - caleope-public
      - caleope-internal

  immich-machine-learning:
    image: ghcr.io/immich-app/immich-machine-learning:release
    container_name: immich-machine-learning
    restart: unless-stopped
    env_file:
      - app.env
      - {{.BaseDir}}/app-config/{{.AppID}}/secrets.env
    volumes:
      - {{.BaseDir}}/app-data/immich/model-cache:/cache
    networks:
      - caleope-internal

  immich-redis:
    image: redis:7-alpine
    container_name: immich-redis
    restart: unless-stopped
    networks:
      - caleope-internal

  immich-db:
    # Image DB officielle Immich : VectorChord + pgvector.
    # ⚠️ pgvecto.rs n'est PLUS fourni : Immich l'a abandonné en v3.0.0 et les
    # bibliothèques existantes ont migré vers VectorChord. Une base restée sur
    # pgvecto.rs (extension « vectors » présente) doit d'abord passer par
    # l'image de transition 14-vectorchord0.4.3-pgvectors0.2.0 AVANT celle-ci.
    # Vérification : select extname from pg_extension → « vectors » absent.
    image: ghcr.io/immich-app/postgres:14-vectorchord1.1.1-pgvector0.8.5
    container_name: immich-db
    restart: unless-stopped
    env_file:
      - app.env
      - {{.BaseDir}}/app-config/{{.AppID}}/secrets.env
    volumes:
      - {{.BaseDir}}/app-data/immich/db:/var/lib/postgresql/data
    networks:
      - caleope-internal

  immich-bootstrap:
    image: alpine:3.22
    container_name: immich-bootstrap
    restart: "no"
    env_file:
      - {{.BaseDir}}/app-config/{{.AppID}}/secrets.env
    entrypoint: ["/bin/sh", "-c", "apk add --no-cache curl python3 >/dev/null 2>&1 && sh /bootstrap.sh"]
    volumes:
      - {{.BaseDir}}/app-config/{{.AppID}}/bootstrap.sh:/bootstrap.sh:ro
    depends_on:
      - immich-server
    networks:
      - caleope-internal

networks:
  caleope-public:
    external: true
  caleope-internal:
    external: true
