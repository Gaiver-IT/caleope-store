services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped
    user: "0:0"
    env_file:
      - ${CALEOPE_APP_CONFIG}/filebrowser/secrets.env
    ports:
      - "${FILEBROWSER_PORT_WEB:-8085}:80"
    volumes:
      - ${CALEOPE_APP_DATA}/filebrowser:/srv
      - ${CALEOPE_APP_DATA}/filebrowser/db/filebrowser.db:/database/filebrowser.db
      - ${CALEOPE_APP_DATA}/filebrowser/conf/.filebrowser.json:/.filebrowser.json
    networks:
      - caleope-public
      - caleope-internal

networks:
  caleope-public:
    external: true
  caleope-internal:
    external: true
